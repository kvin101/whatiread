spring:
  datasource:
    url: jdbc:h2:mem:whatiread-prod-test-${random.uuid};MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver

management:
  tracing:
    sampling:
      probability: ${TRACING_SAMPLE_RATE:1.0}
