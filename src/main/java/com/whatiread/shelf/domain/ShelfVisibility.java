package com.whatiread.shelf.domain;

public enum ShelfVisibility {
    /**
     * Owner and explicit members only; not listed on profile for others.
     */
    SECRET,
    PRIVATE,
    FRIENDS,
    PUBLIC
}
